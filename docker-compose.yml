services:
  mining-dashboard:
    build: .
    container_name: mining-profitability
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./cache:/app/cache
