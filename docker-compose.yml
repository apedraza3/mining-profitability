services:
  mining-dashboard:
    build: .
    container_name: mining-profitability
    restart: unless-stopped
    ports:
      - "127.0.0.1:5000:5000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./cache:/app/cache
